def get_flight_routes(source, destination):

    routes = [
        f"{source} → {destination} via Emirates",
        f"{source} → {destination} via Qatar Airways",
        f"{source} → {destination} via Lufthansa",
        f"{source} → {destination} via Air France"
    ]

    return routes